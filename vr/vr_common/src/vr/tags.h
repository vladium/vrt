#pragma once

#include "vr/meta/tags.h"

//----------------------------------------------------------------------------
namespace vr
{

VR_META_TAG (reader);
VR_META_TAG (writer);

VR_META_TAG (event);
VR_META_TAG (state);
VR_META_TAG (update);
VR_META_TAG (pending);
VR_META_TAG (error);

VR_META_TAG (history);

VR_META_TAG (rate);

VR_META_TAG (timestamp);

VR_META_TAG (filter);
VR_META_TAG (payload);

VR_META_TAG (user_data);
VR_META_TAG (instance);
VR_META_TAG (priority);

VR_META_TAG (key);
VR_META_TAG (value);

VR_META_TAG (begin);
VR_META_TAG (end);

VR_META_TAG (empty);
VR_META_TAG (link);

VR_META_TAG (prev);
VR_META_TAG (next);
VR_META_TAG (parent);

} // end of namespace
//----------------------------------------------------------------------------
